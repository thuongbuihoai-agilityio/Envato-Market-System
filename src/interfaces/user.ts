export type TUser = {
  id: string;
  email: string;
  firstName: string;
  lastName: string;
  password: string;
  createdAt: number;
};

export type TCustomer = {
  id: string;
  name: string;
  avatar: string;
  email: string;
  location: string;
};
