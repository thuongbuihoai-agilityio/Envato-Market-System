export enum PAYMENT_STATUS_ENUM {
  PAID = 'paid',
  UN_PAID = 'unpaid',
}

export enum TRANSACTION_STATUS_ENUM {
  COMPLETED = 'completed',
  PROCESSING = 'processing',
  PENDING = 'pending',
  FAILED = 'failed',
  CANCELED = 'canceled',
}
