// the default stale time of data, after this time all queries will be refetching to update new data
export const DEFAULT_STALE_TIME = 60 * 1000; // 1 minute

export const EXPIRED_DAY = {
  REMEMBER: 30,
  NOT_REMEMBER: 1,
};

export const DEBOUNCE_TIME = 600; // ms

export const SHOW_TIME = 3000;
