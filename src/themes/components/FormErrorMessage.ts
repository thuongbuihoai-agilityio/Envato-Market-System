import { defineStyleConfig } from '@chakra-ui/react';

export const FormError = defineStyleConfig({
  baseStyle: {
    text: {
      position: 'absolute',
      fontSize: 'sm',
      mt: 1,
    },
  },
});
